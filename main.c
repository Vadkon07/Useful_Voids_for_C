#include <stdio.h>
#include "templates.h"

int main() {
	generateRandomNumber(1, 5);
	pauseProgram(5, 1); // If you don't want to see a print output, instead of (5, 1) set (5, 0)
	printDate(1); // Try 1,2,3
}
